<!-- Dashboard Left Sidebar -->
<aside class="sidebar sidebar-left custom-scroll">
    <nav class="d-flex flex-column gap-1 mt-2 px-2">
        <div class="px-3 py-2 text-xs font-bold text-muted text-uppercase tracking-wider d-none d-xl-block">Dashboard</div>
        <a href="/dashboard/overview" class="btn btn-ghost justify-content-start gap-3 w-100">
            <i data-lucide="layout-dashboard" size="18" class="text-primary"></i> <span class="d-none d-xl-inline">Overview</span>
        </a>
        <a href="/dashboard/analytics" class="btn btn-ghost justify-content-start gap-3 w-100">
            <i data-lucide="bar-chart-3" size="18"></i> <span class="d-none d-xl-inline">Analytics</span>
        </a>

        <div class="px-3 py-2 mt-2 text-xs font-bold text-muted text-uppercase tracking-wider d-none d-xl-block">Management</div>
        <a href="/dashboard/management" class="btn btn-ghost justify-content-start gap-3 w-100">
            <i data-lucide="settings-2" size="18"></i> <span class="d-none d-xl-inline">Admin Control</span>
        </a>
        <a href="/dashboard/reports" class="btn btn-ghost justify-content-start gap-3 w-100">
            <i data-lucide="flag" size="18"></i> <span class="d-none d-xl-inline">Reports</span>
        </a>

        <div class="px-3 py-2 mt-2 text-xs font-bold text-muted text-uppercase tracking-wider d-none d-xl-block">System</div>
        <a href="/dashboard/monitoring" class="btn btn-ghost justify-content-start gap-3 w-100">
            <i data-lucide="activity" size="18"></i> <span class="d-none d-xl-inline">Monitoring</span>
        </a>
        <a href="/dashboard/settings" class="btn btn-ghost justify-content-start gap-3 w-100">
            <i data-lucide="sliders" size="18"></i> <span class="d-none d-xl-inline">Global Settings</span>
        </a>

        <div class="px-3 py-2 mt-2 text-xs font-bold text-muted text-uppercase tracking-wider d-none d-xl-block">Exit</div>
        <a href="/" class="btn btn-ghost justify-content-start gap-3 w-100">
            <i data-lucide="log-out" size="18"></i> <span class="d-none d-xl-inline">Back to Site</span>
        </a>
    </nav>
</aside>
