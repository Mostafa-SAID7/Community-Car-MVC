<!-- Left Sidebar -->
<aside class="sidebar sidebar-left">
    <div class="sidebar-content custom-scroll">
        <nav class="d-flex flex-column gap-1 mt-1">
            <a href="/" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="home" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["Home"]</span>
            </a>
            <a href="/qa" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="help-circle" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["QA"]</span>
            </a>
            <a href="/posts" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="pen-tool" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["Posts"]</span>
            </a>
            <a href="/guides" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="book-open" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["Guides"]</span>
            </a>
            <a href="/groups" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="users" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["Groups"]</span>
            </a>
            <a href="/friends" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="user-plus" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["Friends"]</span>
            </a>
            <a href="/news" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="newspaper" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["News"]</span>
            </a>
            <a href="/events" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="calendar" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["Events"]</span>
            </a>
            <a href="/reviews" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="star" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["Reviews"]</span>
            </a>
            <a href="/maps" class="btn btn-ghost justify-content-start gap-3 w-100">
                <i data-lucide="map" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["Maps"]</span>
            </a>
        </nav>
    </div>

    <div class="sidebar-footer">
        <a href="/profile" class="btn btn-ghost justify-content-start gap-3 w-100">
            <i data-lucide="user" size="18"></i> <span class="d-none d-xl-inline">@SharedLocalizer["MyProfile"]</span>
        </a>
    </div>
</aside>
