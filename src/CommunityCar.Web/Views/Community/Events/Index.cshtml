@{
    ViewData["Title"] = "Events";
}

<div class="card glass mb-4">
    <div class="card-body">
        <h2 class="h4 mb-3">Upcoming Events</h2>
        <div class="alert alert-info border-0">
            <i data-lucide="info" size="18" class="me-2"></i> No upcoming events found. Check back later!
        </div>
    </div>
</div>
