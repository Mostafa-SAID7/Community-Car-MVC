@model CommunityCar.Application.Features.Stories.ViewModels.StoryVM
@{
    var currentCulture = System.Globalization.CultureInfo.CurrentCulture.Name;
    var isArabic = currentCulture.StartsWith("ar");
    var displayTitle = Model?.Title ?? "Story";
    var displayCaption = isArabic && !string.IsNullOrEmpty(Model?.CaptionAr) ? Model.CaptionAr : Model?.Caption;
    
    ViewData["Title"] = displayTitle;
    Layout = "~/Views/Shared/_Layout.cshtml";
    var currentUserId = ViewBag.CurrentUserId as Guid?;
}

<div class="min-h-screen bg-background">
    <!-- Header -->
    <div class="bg-card/40 backdrop-blur-xl border-b border-border sticky top-16 z-30">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between py-4">
                <div class="flex items-center gap-3">
                    <a href="/stories" class="flex items-center justify-center w-10 h-10 rounded-xl bg-muted hover:bg-accent transition-colors">
                        <i data-lucide="arrow-left" class="w-5 h-5 text-foreground"></i>
                    </a>
                    <div>
                        <h1 class="text-xl font-bold text-foreground">@displayTitle</h1>
                        <p class="text-sm text-muted-foreground">by @Model?.AuthorName</p>
                    </div>
                </div>
                
                <div class="flex items-center gap-2">
                    @if (currentUserId == Model?.AuthorId)
                    {
                        <button type="button" class="p-2 text-muted-foreground hover:text-foreground transition-colors">
                            <i data-lucide="edit" class="w-4 h-4"></i>
                        </button>
                        <button type="button" class="p-2 text-muted-foreground hover:text-red-500 transition-colors">
                            <i data-lucide="trash-2" class="w-4 h-4"></i>
                        </button>
                    }
                    <button type="button" onclick="shareStory('@Model?.Id')" class="p-2 text-muted-foreground hover:text-foreground transition-colors">
                        <i data-lucide="share" class="w-4 h-4"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>

    @if (Model != null)
    {
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <!-- Main Story Content -->
                <div class="lg:col-span-2 space-y-6">
                    <!-- Story Media -->
                    <div class="bg-card rounded-xl border border-border overflow-hidden">
                        @if (!string.IsNullOrEmpty(Model.MediaUrl))
                        {
                            <div class="aspect-[16/9] relative">
                                @if (Model.Type == CommunityCar.Domain.Enums.StoryType.Video)
                                {
                                    <video controls class="w-full h-full object-cover">
                                        <source src="@Model.MediaUrl" type="video/mp4">
                                        Your browser does not support the video tag.
                                    </video>
                                }
                                else
                                {
                                    <img src="@Model.MediaUrl" alt="@Model.Title" class="w-full h-full object-cover" />
                                }
                                
                                @if (Model.IsHighlighted)
                                {
                                    <div class="absolute top-4 right-4 w-8 h-8 bg-yellow-500 rounded-full flex items-center justify-center">
                                        <i data-lucide="star" class="w-4 h-4 text-white"></i>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="aspect-[16/9] bg-gradient-to-br from-primary/10 to-accent/10 flex items-center justify-center">
                                <div class="text-center">
                                    <i data-lucide="image" class="w-16 h-16 text-muted-foreground mx-auto mb-4"></i>
                                    <p class="text-muted-foreground">Text-only story</p>
                                </div>
                            </div>
                        }
                    </div>

                    <!-- Story Content -->
                    <div class="bg-card rounded-xl border border-border p-6">
                        <!-- Author Info -->
                        <div class="flex items-center gap-3 mb-4">
                            <div class="w-12 h-12 rounded-full overflow-hidden">
                                <img src="@(Model.AuthorAvatar ?? "/images/default-avatar.png")" alt="@Model.AuthorName" class="w-full h-full object-cover" />
                            </div>
                            <div class="flex-1">
                                <div class="font-semibold text-foreground">@Model.AuthorName</div>
                                <div class="text-sm text-muted-foreground">@Model.TimeAgo</div>
                            </div>
                            @if (currentUserId.HasValue && currentUserId != Model.AuthorId)
                            {
                                <button type="button" class="px-4 py-2 bg-primary text-primary-foreground rounded-lg hover:bg-primary/90 transition-colors text-sm">
                                    Follow
                                </button>
                            }
                        </div>

                        <!-- Story Title -->
                        @if (!string.IsNullOrEmpty(Model.Title))
                        {
                            <h2 class="text-2xl font-bold text-foreground mb-4">@Model.Title</h2>
                        }

                        <!-- Story Caption -->
                        @if (!string.IsNullOrEmpty(displayCaption))
                        {
                            <div class="prose prose-sm max-w-none text-foreground mb-4">
                                @Html.Raw(displayCaption.Replace("\n", "<br>"))
                            </div>
                        }

                        <!-- Car Information -->
                        @if (!string.IsNullOrEmpty(Model.CarMake) || !string.IsNullOrEmpty(Model.CarModel) || Model.CarYear.HasValue)
                        {
                            <div class="bg-accent/30 rounded-lg p-4 mb-4">
                                <div class="flex items-center gap-2 mb-2">
                                    <i data-lucide="car" class="w-4 h-4 text-primary"></i>
                                    <span class="font-medium text-foreground">Vehicle Information</span>
                                </div>
                                <div class="text-sm text-muted-foreground">
                                    @if (!string.IsNullOrEmpty(Model.CarMake))
                                    {
                                        <span>@Model.CarMake</span>
                                    }
                                    @if (!string.IsNullOrEmpty(Model.CarModel))
                                    {
                                        <span>@Model.CarModel</span>
                                    }
                                    @if (Model.CarYear.HasValue)
                                    {
                                        <span>(@Model.CarYear)</span>
                                    }
                                </div>
                            </div>
                        }

                        <!-- Location -->
                        @if (!string.IsNullOrEmpty(Model.LocationName))
                        {
                            <div class="flex items-center gap-2 text-sm text-muted-foreground mb-4">
                                <i data-lucide="map-pin" class="w-4 h-4"></i>
                                <span>@Model.LocationName</span>
                            </div>
                        }

                        <!-- Tags -->
                        @if (Model.Tags?.Any() == true)
                        {
                            <div class="flex flex-wrap gap-2 mb-4">
                                @foreach (var tag in Model.Tags)
                                {
                                    <span class="px-3 py-1 bg-primary/10 text-primary rounded-full text-sm font-medium">
                                        #@tag
                                    </span>
                                }
                            </div>
                        }

                        <!-- Story Stats and Actions -->
                        <div class="flex items-center justify-between pt-4 border-t border-border">
                            <div class="flex items-center gap-6 text-sm text-muted-foreground">
                                <span class="flex items-center gap-1">
                                    <i data-lucide="eye" class="w-4 h-4"></i>
                                    @Model.ViewCount views
                                </span>
                                <span class="flex items-center gap-1">
                                    <i data-lucide="heart" class="w-4 h-4"></i>
                                    @Model.LikeCount likes
                                </span>
                                @if (Model.CommentCount > 0)
                                {
                                    <span class="flex items-center gap-1">
                                        <i data-lucide="message-circle" class="w-4 h-4"></i>
                                        @Model.CommentCount comments
                                    </span>
                                }
                            </div>

                            <div class="flex items-center gap-2">
                                @if (currentUserId.HasValue)
                                {
                                    <button type="button" 
                                            onclick="toggleLike('@Model.Id')"
                                            class="flex items-center gap-1 px-3 py-2 rounded-lg hover:bg-accent transition-colors @(Model.IsLikedByUser ? "text-red-500" : "text-muted-foreground")">
                                        <i data-lucide="heart" class="w-4 h-4 @(Model.IsLikedByUser ? "fill-current" : "")"></i>
                                        <span>Like</span>
                                    </button>
                                }
                                <button type="button" 
                                        onclick="shareStory('@Model.Id')"
                                        class="flex items-center gap-1 px-3 py-2 text-muted-foreground rounded-lg hover:bg-accent transition-colors">
                                    <i data-lucide="share" class="w-4 h-4"></i>
                                    <span>Share</span>
                                </button>
                            </div>
                        </div>
                    </div>

                    <!-- Comments Section -->
                    @if (Model.AllowComments)
                    {
                        <div class="bg-card rounded-xl border border-border p-6">
                            <h3 class="text-lg font-semibold text-foreground mb-4">Comments</h3>
                            
                            @if (currentUserId.HasValue)
                            {
                                <!-- Add Comment Form -->
                                <div class="flex gap-3 mb-6">
                                    <div class="w-10 h-10 rounded-full overflow-hidden bg-muted">
                                        <img src="/images/default-avatar.png" alt="You" class="w-full h-full object-cover" />
                                    </div>
                                    <div class="flex-1">
                                        <textarea id="commentText" 
                                                  rows="3" 
                                                  placeholder="Write a comment..."
                                                  class="w-full px-3 py-2 bg-background border border-border rounded-lg focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-primary resize-none"></textarea>
                                        <div class="flex justify-end mt-2">
                                            <button type="button" 
                                                    onclick="addComment('@Model.Id')"
                                                    class="px-4 py-2 bg-primary text-primary-foreground rounded-lg hover:bg-primary/90 transition-colors">
                                                Post Comment
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            }

                            <!-- Comments List -->
                            <div id="commentsList" class="space-y-4">
                                <!-- Comments will be loaded here -->
                                <div class="text-center py-8 text-muted-foreground">
                                    <i data-lucide="message-circle" class="w-8 h-8 mx-auto mb-2"></i>
                                    <p>No comments yet. Be the first to comment!</p>
                                </div>
                            </div>
                        </div>
                    }
                </div>

                <!-- Sidebar -->
                <div class="space-y-6">
                    <!-- Story Info -->
                    <div class="bg-card rounded-xl border border-border p-6">
                        <h3 class="text-lg font-semibold text-foreground mb-4">Story Info</h3>
                        
                        <div class="space-y-3 text-sm">
                            <div class="flex justify-between">
                                <span class="text-muted-foreground">Created</span>
                                <span class="text-foreground">@Model.CreatedAt.ToString("MMM dd, yyyy")</span>
                            </div>
                            @if (Model.ExpiresAt.HasValue)
                            {
                                <div class="flex justify-between">
                                    <span class="text-muted-foreground">Expires</span>
                                    <span class="text-foreground">
                                        @{
                                            var timeLeft = Model.ExpiresAt.Value - DateTime.UtcNow;
                                            if (timeLeft.TotalHours > 1)
                                            {
                                                <span>@((int)timeLeft.TotalHours)h left</span>
                                            }
                                            else if (timeLeft.TotalMinutes > 0)
                                            {
                                                <span>@((int)timeLeft.TotalMinutes)m left</span>
                                            }
                                            else
                                            {
                                                <span class="text-red-500">Expired</span>
                                            }
                                        }
                                    </span>
                                </div>
                            }
                            <div class="flex justify-between">
                                <span class="text-muted-foreground">Visibility</span>
                                <span class="text-foreground">@Model.Visibility</span>
                            </div>
                            <div class="flex justify-between">
                                <span class="text-muted-foreground">Type</span>
                                <span class="text-foreground">@Model.Type</span>
                            </div>
                        </div>
                    </div>

                    <!-- Related Stories -->
                    <div class="bg-card rounded-xl border border-border p-6">
                        <h3 class="text-lg font-semibold text-foreground mb-4">More from @Model.AuthorName</h3>
                        
                        <div id="relatedStories" class="space-y-3">
                            <!-- Related stories will be loaded here -->
                            <div class="text-center py-4 text-muted-foreground text-sm">
                                Loading related stories...
                            </div>
                        </div>
                    </div>

                    <!-- Share Options -->
                    <div class="bg-card rounded-xl border border-border p-6">
                        <h3 class="text-lg font-semibold text-foreground mb-4">Share Story</h3>
                        
                        <div class="grid grid-cols-2 gap-3">
                            <button type="button" 
                                    onclick="shareToSocial('twitter')"
                                    class="flex items-center justify-center gap-2 p-3 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition-colors">
                                <i data-lucide="twitter" class="w-4 h-4"></i>
                                <span class="text-sm">Twitter</span>
                            </button>
                            <button type="button" 
                                    onclick="shareToSocial('facebook')"
                                    class="flex items-center justify-center gap-2 p-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
                                <i data-lucide="facebook" class="w-4 h-4"></i>
                                <span class="text-sm">Facebook</span>
                            </button>
                            <button type="button" 
                                    onclick="copyStoryLink()"
                                    class="flex items-center justify-center gap-2 p-3 bg-muted text-foreground rounded-lg hover:bg-accent transition-colors">
                                <i data-lucide="link" class="w-4 h-4"></i>
                                <span class="text-sm">Copy Link</span>
                            </button>
                            <button type="button" 
                                    onclick="reportStory('@Model.Id')"
                                    class="flex items-center justify-center gap-2 p-3 bg-red-500 text-white rounded-lg hover:bg-red-600 transition-colors">
                                <i data-lucide="flag" class="w-4 h-4"></i>
                                <span class="text-sm">Report</span>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
            <div class="text-center">
                <div class="w-16 h-16 bg-muted rounded-full flex items-center justify-center mx-auto mb-4">
                    <i data-lucide="alert-circle" class="w-8 h-8 text-muted-foreground"></i>
                </div>
                <h2 class="text-2xl font-bold text-foreground mb-2">Story Not Found</h2>
                <p class="text-muted-foreground mb-6">The story you're looking for doesn't exist or has been removed.</p>
                <a href="/stories" 
                   class="inline-flex items-center gap-2 px-4 py-2 bg-primary text-primary-foreground rounded-lg hover:bg-primary/90 transition-colors">
                    <i data-lucide="arrow-left" class="w-4 h-4"></i>
                    Back to Stories
                </a>
            </div>
        </div>
    }
</div>

@section Scripts {
    <script src="~/js/stories.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            loadRelatedStories();
        });

        function toggleLike(storyId) {
            fetch(`/api/stories/${storyId}/like`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    location.reload(); // Simple reload for now
                }
            })
            .catch(error => {
                console.error('Error toggling like:', error);
            });
        }

        function shareStory(storyId) {
            const url = window.location.href;
            if (navigator.share) {
                navigator.share({
                    title: '@Model?.Title',
                    text: 'Check out this story!',
                    url: url
                });
            } else {
                copyToClipboard(url);
            }
        }

        function shareToSocial(platform) {
            const url = encodeURIComponent(window.location.href);
            const text = encodeURIComponent('@Model?.Title');
            
            let shareUrl = '';
            switch (platform) {
                case 'twitter':
                    shareUrl = `https://twitter.com/intent/tweet?text=${text}&url=${url}`;
                    break;
                case 'facebook':
                    shareUrl = `https://www.facebook.com/sharer/sharer.php?u=${url}`;
                    break;
            }
            
            if (shareUrl) {
                window.open(shareUrl, '_blank', 'width=600,height=400');
            }
        }

        function copyStoryLink() {
            copyToClipboard(window.location.href);
        }

        function copyToClipboard(text) {
            navigator.clipboard.writeText(text).then(() => {
                showNotification('Link copied to clipboard!', 'success');
            });
        }

        function addComment(storyId) {
            const commentText = document.getElementById('commentText');
            if (!commentText.value.trim()) return;

            // In a real implementation, you would make an API call here
            showNotification('Comment functionality coming soon!', 'info');
            commentText.value = '';
        }

        function reportStory(storyId) {
            if (confirm('Are you sure you want to report this story?')) {
                fetch(`/api/stories/${storyId}/report`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ reason: 'Inappropriate content' })
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        showNotification('Story reported successfully', 'success');
                    }
                })
                .catch(error => {
                    console.error('Error reporting story:', error);
                });
            }
        }

        function loadRelatedStories() {
            const container = document.getElementById('relatedStories');
            
            fetch(`/stories/author/@Model?.AuthorId?pageSize=5`)
                .then(response => response.text())
                .then(html => {
                    // In a real implementation, you would parse and display related stories
                    container.innerHTML = '<div class="text-center py-4 text-muted-foreground text-sm">No other stories found</div>';
                })
                .catch(error => {
                    console.error('Error loading related stories:', error);
                    container.innerHTML = '<div class="text-center py-4 text-muted-foreground text-sm">Error loading stories</div>';
                });
        }

        function showNotification(message, type = 'info') {
            // Simple notification - in a real app you'd use a proper notification system
            alert(message);
        }
    </script>
}